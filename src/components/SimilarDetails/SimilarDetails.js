import React from 'react';
import {useParams} from "react-router-dom";

const SimilarDetails = () => {

    let {id: id} = useParams();

    return (
        <div>

        </div>
    );
};

export {SimilarDetails};